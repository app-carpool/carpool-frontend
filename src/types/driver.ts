type Driver = {
  licenseClass: string;
  licenseExpirationDate: string; // formato ISO (YYYY-MM-DD)
  birthDate: string;             
  addressStreet: string;
  addressNumber: string;
  locality: string;
};